package com.example.oneentry.model

/**
 * Sorting marker
 *
 * @param DESK Sorting marker in descending order
 * @param ASK Ascending sorting marker
 */
enum class SortDirection {
    DESK,
    ASK
}